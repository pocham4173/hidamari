# AI引き継ぎノート（まいにこ）

> このファイルは公開されています。個人情報、テスト参加者情報、市との非公開のやり取り、認証情報、具体的なセキュリティ弱点は書きません。

## 役割
- **りえ**：目的と希望を伝え、最終決定・公開承認をする
- **ChatGPT**：利用者目線・企画・安全性を整理し、このファイルだけを更新する
- **Claude Code**：コードの実装・テスト・変更用ブランチ・Pull Requestを担当する

## 絶対ルール
1. ChatGPT／Codex／Claude Codeは、りえから明確な実行依頼がある場合だけアプリ本体を変更します。
2. アプリコードは main へ直接上書きせず、変更用ブランチとPull Requestを使います。
3. りえはコードを手動アップロードせず、Pull Requestの最終承認を行います。
5. Firebase、認証、Firestore、Security Rules、既存データ形式、localStorageキーを推測で変更しません。
6. りえの明確な承認なしに公開版へマージしません。
7. 書類ポケット・AI本格接続・災害カードは、本人テスト完了まで実装しません。おまもりタグは内部試作品のみとし、本人同意・実通知・乱用防止・完全削除・通し検査が完成するまで販売・配布・自治体実証に使用しません。
8. POCHAM FIVEの実行依頼、またはりえの明確な開始指示を作業開始の合図にします。
9. 新しい依頼を始める前に、前回の依頼を「完了」または「保留」にします。
10. Claude Codeは作業後、結果・日付・変更ファイル・ブランチ／PRの有無を「Claude Codeからの回答」欄に短く追記します。公開できない内容は詳細を書かず、その旨だけ記録します。

---

## 現在の状態
- 更新日：2026-08-30
- リポジトリ：pocham4173/hidamari
- 公開ブランチ：main
- 状態：PR #31はmainへ反映済み。QRおまもりタグの安全な内部試作を変更用ブランチで確認中
- 注意：現段階のQR操作はプッシュ通知・電話・メールではなく、家族がアプリを開いたときに確認できる履歴記録です

## りえの希望
- ChatGPTとClaude Codeの間の長いコピー作業を減らす
- Claude Codeがコード修正とGitHub操作を担当する
- ChatGPTが企画・利用者目線・安全性・実装依頼を整理する
- 最終決定はりえが行う

## ChatGPTからClaude Codeへの連絡
**状態：2026-08-30 FIVE/FRAME会議反映**

最優先は、(1)保存成功後だけ完了表示、(2)同一操作のサーバー側重複防止、(3)本人・家族・管理者の権限分離、(4)タグ・通知・招待を含む完全削除、(5)複数端末・別グループの通し検査です。
全国展開は、全国共通コアと自治体別相談データを分離し、上田市を最初の地域設定として扱います。

## Claude Codeからの回答
**状態：回答済み**

- 2026-08-24：この共有ノート（AI_HANDOFF.md）を読めることを確認しました。
- 指示のとおり、アプリ本体（index.html / manifest.json / sw.js / 画像）は変更していません。ブランチとPull Requestも作成していません。
- 変更したのは AI_HANDOFF.md のこの欄のみで、絶対ルール3にもとづき main へ直接コミットしています。
- 次の依頼をお待ちしています。

- 2026-08-26：GitHub 上で Claude を呼び出せるようにする設定（GitHub Actions）を追加しました。
- 変更ファイル：`.github/workflows/claude.yml`（新規）、`AI_HANDOFF.md`（この欄のみ）。アプリ本体（index.html / manifest.json / sw.js / 画像）は変更していません。
- ブランチ：`claude/hidamari-github-actions-uo2fyp` / Pull Request：あり（絶対ルール3にもとづき main へ直接コミットしていません）。
- 動く条件：Issue か Pull Request のコメントに「@claude」と書いたときだけです。コードの push などでは動きません。
- 認証は GitHub Secret「CLAUDETOKEN」（claude setup-token で発行した Claude Code の OAuth トークン）を使います。トークンの値はこのノートには書きません。
- 2026-08-26（追記）：りえの指示で、認証を OAuth トークン1本に整理し、さらに「コメントを書いたのが pocham4173 本人のときだけ動く」制限を追加しました。同じ Pull Request（#21）に反映しています。

## りえの最終決定
**状態：確認待ち**

（りえの決定をここに記録）
