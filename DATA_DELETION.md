# 共有データ削除の仕様と確認範囲

この文書は `household-deletion.js` と対応する Firestore ルールの仕様です。アプリ内の名称は「共有データを削除」とし、Firebase Authentication のアカウントや、紙・別端末のコピーまで消えたという表示はしません。本機能には有料の Cloud Functions を追加しません。

## 管理者と削除開始

共有データ全体を削除できるのは `groups/{groupId}.createdBy` の利用者だけです。家族として承認されているだけでは実行できません。UI は対象と影響を説明し、管理者に「共有データを削除」と入力してもらってから API を呼びます。Firestore ルールでも管理者を確認します。

最初にサーバー上の管理者を確認し、端末に再開情報を保存した後、トランザクションで `deletionState: 'deleting'` と `deletionStartedAt: serverTimestamp()` を書き込みます。この状態への移行は取り消せません。ルールは削除中の通常の新規書込み・変更・招待参加・おまもりタグ通知を拒否します。管理者の権限は作成者情報で判定するので、途中で管理者の member 文書を削除しても清掃を続けられます。

## サーバー側で削除する範囲

| 対象 | 処理 |
| --- | --- |
| `groups/{groupId}/events/*` | 本人・家族のイベントを含め全件 |
| `groups/{groupId}/yotei/*` | 登録者によらず全件 |
| `groups/{groupId}/members/*` | 承認済み・承認待ちを含め全件 |
| `groups/{groupId}/settings/watchTag` | タグ設定 |
| `invites/*` | `groupId` が対象と一致する全件 |
| `watchTags/*/alerts/*` | 対象グループに属するタグの通知を先に全件 |
| `watchTags/*` | 通知の空確認が済んだ対象タグを全件 |
| `accounts/*` | `groupId` が対象と一致する復旧用ポインタ。管理者以外を先に削除 |
| `groups/{groupId}` と管理者の `accounts/{uid}` | 全件の空確認後、同じ最終トランザクションで削除 |

管理者の `accounts/{uid}` は最終トランザクションで読み直します。他の端末で別グループの復旧先へ変更された場合、そのポインタは削除しません。管理者のポインタを最終処理まで残すことで、削除途中でも登録済みの復旧方法で同じ UID に戻り、処理を再開できます。別の利用者の Firebase Authentication アカウントを削除する処理は含みません。

対象は、現行アプリの書込み先とルールで許された上記スキーマです。将来サブコレクションや保存先を追加するときは、削除処理・ルール・テストを一緒に更新する必要があります。管理者 SDK や過去の別バージョンによって作成された未知の保存先は自動探索できません。過去に親グループや親タグだけが消されて残った孤立データは、このクライアントから網羅的に確認できないため、運営側での監査・清掃が必要です。

## 中断と再開

1. 毎回サーバーから残存データを最大 100 件ずつ読み、小分けのバッチで削除します。削除済みカーソルや端末に記録した件数を完了の根拠にしません。
2. タグ通知が空であることを確認してから、親タグを消します。グループ配下・招待・タグ・復旧ポインタもサーバーで再確認します。復旧ポインタは最後に消す管理者自身の 1 件だけを許容します。
3. グループ本体は最後です。最終トランザクションが成功した後も、サーバーからグループの不存在を確認して初めて完了を返します。
4. 通信切断、応答待ちの期限超過、権限エラー、端末保存エラー、空確認の失敗は「削除途中」です。完了表示はしません。同じ管理者・同じグループで API を再実行すると残存データから再開します。
5. 最終処理の応答だけが途絶えた場合は、端末に残った `finalizing` 状態とサーバー上の不存在を確認して完了を判定します。初回にグループが見つからないだけでは、すべて消えたとは判定しません。

再開情報は `localStorage` の `mainico_deletion_pending_v1` に保存し、グループ ID、管理者 UID、段階、応答を確認できた削除件数だけを保持します。本文・氏名・連絡先は保存しません。表示件数は確認済みの処理数であり、応答を失った処理を含む監査用の全削除件数ではありません。保存に失敗した場合は処理を止めます。中断中はブラウザデータを手作業で消さず、管理者の復旧経路を確保します。

## 端末・アカウント・紙の扱い

- サーバーの削除完了と、この端末の表示設定・災害 QR・認証セッションの片付けは別の処理です。呼出側の UI は、サーバー完了後に端末内の関連情報を清掃し、その清掃失敗を隠さない必要があります。
- `localCheckpointCleared: false` のときは、サーバー削除は確認済みでも端末の再開情報が消せていません。端末内の削除を再試行できる案内を表示します。
- 別端末のブラウザ保存、スクリーンショット、印刷した災害 QR、第三者が転記した情報は遠隔削除できません。手元に残る紙や画像は利用者が処分し、他の家族にも端末内情報の整理を依頼します。
- Firebase Authentication のログイン情報は別管理です。本処理では本人・他の家族の認証アカウントを消しません。認証アカウントを閉じる場合は、対象・共有グループへの影響を別途確認した上で、本人の認証を伴う退会手続きを行います。
- Firebase の基盤ログ、バックアップ、外部に出力済みの情報まで物理的に即時消去したと約束する機能ではありません。

## 呼出 API

```js
const deletion = MainicoDeletion.create({
  db,
  auth,
  storage: localStorage,
  serverTimestamp: () => firebase.firestore.FieldValue.serverTimestamp(),
  isOnline: () => navigator.onLine !== false,
  onProgress: state => renderDeletionProgress(state)
});

const pending = deletion.getPending(); // null または { groupId, uid, stage, deletedCount, version }
const result = await deletion.run({
  groupId: selectedGroupId,
  confirmation: enteredConfirmation // 正しい値は MainicoDeletion.CONFIRMATION
});
// result: { status: 'complete', groupId, deletedCount, localCheckpointCleared }
```

`onProgress` の `status` は `checking`, `deleting`, `verifying`, `finalizing`, `complete`, `paused` です。エラーは `code` と日本語の `message`、開始後は `deletionState` を持ちます。Firebase 側のエラーコードは `backendCode` に保持し、UI で生の内部エラーや個人データを表示しません。サーバー読取り・書込みの応答期限は既定 20 秒です。期限超過は書込みの取消しを意味せず、後からサーバーで完了する可能性があるため、再実行時も残存状態を確認します。

## 無料運用の範囲と確認

Spark の利用枠内で実行するクライアント処理です。読み取り・削除の利用量は発生し、無料枠を使い切った場合も完了扱いにせず、利用可能になってから再開します。従量課金プランへの変更を自動で行う処理はありません。大量データやブラウザを閉じた後の確実な継続実行が必要になる段階では、運営者が実行する信頼できるサーバー側の削除ジョブへ移行する判断が必要です。

状態テストは `node tests/household-deletion.test.mjs` で実行します。複数バッチ、他グループ隔離、管理者限定、同意入力、オフライン、キャッシュ拒否、再開情報の保存失敗、途中再開、タグ通知の順序、空確認失敗、最終応答喪失、アカウント変更、別グループの復旧先保護、端末清掃失敗を検証します。Firestore ルールのエミュレータ検証は別に実施し、模擬 API の状態テストだけで権限が安全とは判断しません。実機での中断・再読込・復旧・表示確認は公開前の確認に残ります。

公式資料（2026-09-15 確認）:

- [Firebase: データの削除](https://firebase.google.com/docs/firestore/manage-data/delete-data) — コレクションは個々の文書を削除する必要があり、大規模な削除は信頼できるサーバー環境での実行を推奨。
- [Firebase: データモデル](https://firebase.google.com/docs/firestore/data-model) — SDK から親文書を削除してもサブコレクションは自動削除されない。

## 自分のログイン用アカウントを削除する

`account-deletion.js` は共有データ削除とは別の自己退会処理です。「設定」または共有データ削除後の画面から開きます。対象メールアドレスを表示し、復旧を設定した本人は現在のパスワードで再認証します。既存パスワードには新規登録時の12文字条件を押し付けません。メール未登録の匿名利用者は現在の認証セッションを使います。認証サービスが直近のログインを要求した場合は成功扱いにせず止めます。

1. 本人が所有する `groups` を `createdBy == 現在UID` でサーバー検索し、残る場合は先にすべての共有データを削除するよう案内します。復旧先 `accounts/{uid}` が残る場合も参加解除・共有削除を先に行います。復旧先を作る前の旧管理世帯も検索対象です。
2. 再認証後、`accountClosures/{uid}` に `requestedAt` のみをサーバー時刻で作成します。これは不可逆な利用停止です。氏名・メール・パスワード・認証トークンは保存しません。新世帯の作成、通常の記録、招待参加、復旧先の登録、外部からのおまもりタグ通知を、そのUIDからは拒否します。同一バッチでの閉鎖と新世帯作成も `getAfter` で拒否します。
3. ロック後に所有世帯と復旧先がないことを再確認します。競合により残った所有世帯があれば認証を削除せず、再起動時にその共有データの整理へ案内します。閉鎖中でも本人の退会と管理者による共有削除は可能です。
4. 利用者が別の最終確認欄に「アカウントを削除」と入力した後、ロック・所有世帯・復旧先・同じUIDであることを再確認し、現在の `user.delete()` を実行します。準備後に画面を閉じた場合は「未完了・通常利用は停止・再開できる」と表示します。
5. 削除成功後はログイン登録を削除した旨だけを表示します。Auth状態変更や再読込みで新しい匿名アカウントを自動作成しません。明示的に「新しく利用を始める」を選ぶと、新規利用へ進めます。通信障害でAuth削除の応答を受け取れなかった場合は、削除できていないと断定せず、完了未確認として運営による確認を案内します。

パスワードは入力と再認証呼出しの間だけメモリーで扱い、成否にかかわらず入力欄を空にします。削除開始から各段階でUID変更を拒否します。別の家族のAuth登録を消す権限は付与しません。

### 閉鎖記録の最終整理

Firebase IDトークンは最大約1時間有効なので、Auth登録を消した直後に閉鎖記録を消しません。アプリ利用者は閉鎖記録を上書き・削除できません。運営はAuth登録の不存在を確認し、その不存在を信頼できる手順で最初に観測した時刻（または確実なAuth削除完了時刻）から24時間以上待ち、再度不存在と残存データを確認した上で最終整理します。

**`requestedAt + 24時間` だけでは整理しません。** `requestedAt` は準備開始時刻であり、数日後にAuth削除を完了するケースがあるためです。Auth登録が残る場合、完了日時が不明な場合、未整理の世帯・記録がある場合は閉鎖記録を維持します。24時間の待機はこのアプリの運用上の余裕であり、Firebaseが要求する保存期間を意味しません。最終整理は無条件の自動タイマーではありません。

### 利用者が最後に確認すること

1. 家庭全体の利用を終える管理者は共有データ削除を完了します。自分だけ利用をやめる参加者は参加解除します。
2. 自分のログイン用アカウントを別の確認画面で削除します。他の家族はそれぞれのアカウントについて判断します。
3. 印刷した災害QRを読み取れないよう切断して廃棄します。既に撮影・転記された内容を遠隔で消すことはできません。
4. 自分と家族が管理する別端末のサイト保存、写真、スクリーンショットを各自で整理します。この端末のアプリ内保存の削除は、ブラウザーの全保存領域の物理消去を保証しません。
5. 旧版で別の家庭に残った本人のmemberやUID付き記録、親文書がなくなった孤立データは運営者へ整理を依頼します。アプリのAuth自己削除はこれらの不存在証明ではありません。運営がデータの帰属と他の家族への影響を確認して扱います。

以上は利用者が実施する終了手順です。チェックしたこと自体をサーバー削除の証明や第三者の複製消去の証明にはしません。

追加検証：`tests/account-deletion.test.mjs`（残存世帯・復旧先、通信・再認証失敗、UID変更、閉鎖直後の競合、最終確認、再開）、`tests/account-closure-integration.test.mjs`（実SDKとFirestoreルールの結合、他人の閉鎖記録拒否、旧token書込拒否、同一batch競合、旧所有世帯の整理）、`tests/household-ui.test.mjs`（終了後の匿名再作成防止、未完了表示、パスワード消去）。実際のAuth削除・メール到達・実端末の操作は模擬試験だけでは確認済みとしません。

公式資料（2026-09-15確認）：[Firebase: ユーザーの管理](https://firebase.google.com/docs/auth/web/manage-users)（削除と再認証）、[Firebase: セッション管理](https://firebase.google.com/docs/auth/admin/manage-sessions)（IDトークン有効期間・ユーザー削除と更新トークン）。


## 端末内のお薬手帳の控え（追加）

写真・PDFはFirestoreへ送信せず、このブラウザーのIndexedDBに保存します。家庭の共有削除・自分の参加解除・接続済みでない端末の初期化では、操作前に取得した本人UIDと家庭IDに一致する控えの削除完了を待ちます。Auth自己削除では、削除前の本人UIDに一致する全家庭の控えを整理します。ほかのアカウントの控えは自動では削除しません。削除に失敗した場合、端末保存まで完了したとは表示しません。

入口・終了画面の「端末内の控えを削除」では、確認を経てこのブラウザーの全家庭・全アカウントの控えを削除できます。書き出したファイル・紙・他端末は対象外です。

アカウント復旧で端末内の控えをクラウドから復元することはありません。同じブラウザーに残っていて同じUID・家庭IDへ戻った場合に限り再表示できます。復旧時は控え自体を消さず、別アカウント・家庭の画面には表示しません。画面停止・ログイン切替で閲覧と保存を中止し、画像URLを破棄します。これは端末ロックやアプリ単位の暗号化を代替するものではありません。
